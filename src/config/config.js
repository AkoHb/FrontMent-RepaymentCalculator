module.exports =  {
    languageButtons : {
        langButtonsCount: 3,            // count language buttons at the top of the user block
        minColumnSize: 250,             // declare min size to 250px to correct display on small screens
    },
    minBlockWidth: 250,                 // declare min block size to make valid grid on small screens
    minBlockHeight: 600,                // pass the min height measured  by  figma
    padding: '1rem',                    // main padding in two blocks (user input and results)
    minAmount: 0,
    maxAmount: 999999999,
    placeholderAmount: "350 000",
    minTerm: 0,
    maxTerm: 125,
    placeholderTerm: "12",
    minRate: 0,
    maxRate: 100,
    placeholderRate: "5.25",

    

}